extends layout.pug

block content



  article.portfolio-project-thumbs
    .projects-box.box.grid
        each art in arts
          .cell
            if art.image_urls && art.image_urls.length
              each url in art.image_urls
                img(src=url, alt="artwork")




