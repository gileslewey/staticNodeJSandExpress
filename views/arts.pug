extends layout.pug

block artpic

  nav.nav
    a(href='/')
      span &larr;
      |  Back

  article.portfolio-projects
    .inner-wrapper.flex-row-wrap.two-col
      .project-info-box.box

        h2 #{art_name}


    .project-img-box.box

      img.art-img(src=`${image_urls[0]}`, alt='')

    nav.nav
      a(href='/')
        span &larr;
        |  Back
