extends layout.pug

block content

  article.portfolio-intro

    h1 My Portfolio

    p Websites
      article.portfolio-project-thumbs
        .projects-box.box.grid
          .webcell
            a(href='https://www.characterwork.com')
              img.thumbnail(src='/static/images/ccbl.png', alt='character website image')
              h5 characterwork.com

            a(href=`https://www.mtdwi.com`)
              img.thumbnail(src='/static/images/mtdwi.png', alt='mtdwi.com site image')
              h5 mtdwi.com

    p Print-work, Design and Illustration
      article.portfolio-project-thumbs
        .projects-box.box.grid
          .webcell
            a(href='/print')
              img.thumbnail(src='static/images/cultureCrop.png', alt='Culture crop sample page')
              h5 Print-work
            a(href='/art')
              img.thumbnail(src='static/images/4ballsCrop.png', alt='4 balls sample page')
              h5 Design and Illustration
        
    p Here are some of the javascript projects I have been working on. 

  article.portfolio-project-thumbs
    .projects-box.box.grid

      each project in projects 
        .cell
          a(href=`/project/${project.id}`)
            img.thumbnail(src=`${project.image_urls[0]}`, alt=`${project.project_name}`)
            h5 #{project.project_name}
            p #{project.description}





